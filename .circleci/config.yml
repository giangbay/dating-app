version: 2

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build and push Docker images
          command: |
            docker build -t dating-app-backend ./backend
            docker build -t dating-app-frontend ./frontend
            docker build -t dating-app-chat ./chat-server
            # Add Docker push commands when ready for deployment

  test:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Run tests
          command: |
            echo "Running tests..."
            # Add test commands

workflows:
  version: 2
  build_and_test:
    jobs:
      - test
      - build:
          requires:
            - test
